{% extends 'base.html.twig' %}
{% block title %}Genus {{ name }}{% endblock %}
{% block body %}

    <h1>{{ chirulao }}</h1>

    <h2> The Genus {{ name }}</h2>

    <ul>
        {% for note in notes %}
            <li>{{ note }}</li>
        {% endfor %}
    </ul>

    {{ dump() }}
{% endblock %}